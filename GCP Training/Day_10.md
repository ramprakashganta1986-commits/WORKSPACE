## SDK = Software Developmet Kit

# To create a role or access a service we have two options
  * SDK/CLI
  * YAML
  * Terraform

  # gcloud commands
    * gcloud auth list
    * gcloud auth login
    * gcloud set project PROJECT ID
    * gcloud iam roles create customrole --project=project-1b5f04dd-782c-43ae-9bc --file=customrole.yaml